
{% extends "AppBundle:Layout:templateadmin.html.twig" %}



{% block body %}

<h1>Gestions des noms des joueurs</h1>




<table class="table table-hover">

<thead>
<tr>
  <th>Joueurs</th><th>Date de création</th><th>Date de modification</th> <th>Modfication</th>
</tr>
</thead>
<tbody>

    {% for player in listplayers %}
    <tr>  <td>{{ player.libelle }}</td><td>{{ player.createdAt|date("d/m/Y\\ H:i") }}</td> <td>{{ player.updateAt|date("d/m/Y \\ H:i") }}</td><td><a href="{{ path('update_player', {'id': player.id})}}"><i class="fa fa-pencil-square" aria-hidden="true"></i></a>
           <a href="{{ path('delete_player', {'id': player.id})}}"><i class="fa fa-trash" aria-hidden="true"></i></a> </td>    </tr>
    {% endfor %}


</tbody>
</table>

<p> Formulaire</p>
<div class="well">

  {{ form(form) }}

</div>


{% endblock %}
